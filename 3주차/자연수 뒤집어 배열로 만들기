function solution(n) {
    var answer = [];
    // n = 12345
    // answer = [5,4,3,2,1]
    // n을 배열로 만든 후 reverse()를 이용해 배열의 반대부터 순회를 돌려서 숫자로 변환 후 push
    String(n).split('').reverse().forEach((item)=> answer.push(Number(item)));
    
    return answer
}

function solution(arr, divisor) {
    var answer = [];
    arr.forEach((item)=> {
        if(item % divisor === 0){
        answer.push(item)
        }
    })
    return answer.length !== 0 ? answer.sort((a,b) => a - b) : [-1] 
}
